keymode=default

# reload config (picks up noctalia-rendered colors.conf after a theme switch)
bind=SUPER,r,reload_config

# apps
bind=SUPER,t,spawn,kitty
bind=SUPER,space,spawn,noctalia msg panel-toggle launcher
bind=NONE,Print,spawn_shell,grim -g "$(slurp)" - | satty -f -
bind=SUPER,c,spawn,noctalia msg panel-toggle clipboard

# window
bind=SUPER,q,killclient
bind=SUPER,v,togglefloating
bind=SUPER,f,togglemaximizescreen
bind=SUPER+SHIFT,f,togglefullscreen

# focus (arrows + vim, mirrors hyprland binds.lua)
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down

# move windows (arrows + vim)
bind=SUPER+ALT,Left,exchange_client,left
bind=SUPER+ALT,Right,exchange_client,right
bind=SUPER+ALT,Up,exchange_client,up
bind=SUPER+ALT,Down,exchange_client,down
bind=SUPER+ALT,h,exchange_client,left
bind=SUPER+ALT,l,exchange_client,right
bind=SUPER+ALT,k,exchange_client,up
bind=SUPER+ALT,j,exchange_client,down

# monitor focus
bind=SUPER+SHIFT,h,focusmon,left
bind=SUPER+SHIFT,l,focusmon,right

# tags 1-9 (mangowm uses tags not workspaces — no tag 10)
bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0
bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0
bind=SUPER+SHIFT,7,tag,7,0
bind=SUPER+SHIFT,8,tag,8,0
bind=SUPER+SHIFT,9,tag,9,0

# mouse
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize

# quit
bind=SUPER,m,quit

# layout switcher submap (super+shift+l enters, escape exits)
keymode=layout
bind=d,setlayout,dwindle
bind=m,setlayout,tile
bind=s,setlayout,scroller
bind=o,setlayout,monocle
bind=Escape,setkeymode,default
keymode=default